<Window x:Class="UnisonAlmacen.App.Views.NotaListView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Lista de Notas" Height="450" Width="800">
    <Grid>
        <ListView ItemsSource="{Binding Notas}" Margin="10">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="ID" DisplayMemberBinding="{Binding Id}" Width="50"/>
                    <GridViewColumn Header="Título" DisplayMemberBinding="{Binding Titulo}" Width="200"/>
                    <GridViewColumn Header="Contenido" DisplayMemberBinding="{Binding Contenido}" Width="500"/>
                </GridView>
            </ListView.View>
        </ListView>
        <Button Content="Cargar Notas" Command="{Binding CargarNotasCommand}" Width="100" Height="30" VerticalAlignment="Bottom" Margin="10"/>
    </Grid>
</Window>