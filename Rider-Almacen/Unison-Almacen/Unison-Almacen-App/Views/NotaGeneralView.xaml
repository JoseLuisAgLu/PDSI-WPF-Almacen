<Page x:Class="Unison_Almacen_App.Views.NotaGeneralView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:local="clr-namespace:Unison_Almacen_App.Views"
      xmlns:viewModel="clr-namespace:Unison_Almacen_App.ViewModel"
      d:DataContext="{d:DesignInstance viewModel:NotaGeneralViewModel}"
      mc:Ignorable="d"
      Title="NotaGeneralView" Height="511" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Formulario -->
        <StackPanel Grid.Row="0" Margin="150 0 150 0">
            <TextBlock Text="Notas"
                       Style="{StaticResource TitleText}" />

            <!-- Título de la nota -->
            <Label Content="Título" />
            <ui:TextBox Text="{Binding Path=Nota.Titulo, Mode=TwoWay}" />

            <!-- Contenido de la nota -->
            <Label Content="Contenido" />
            <ui:TextBox Text="{Binding Path=Nota.Contenido, Mode=TwoWay}" />

            <!-- Selección de Color -->
            <Label Content="Color" />
            <ComboBox ItemsSource="{Binding Colores}" 
                      SelectedItem="{Binding Nota.Color, Mode=TwoWay}" />

            <!-- Botón para agregar/modificar una nota -->
            <ui:Button Content="{Binding TxtBotonFormulario, Mode=OneWay}"
                       Style="{StaticResource GreenPrimaryButton}"
                       Command="{Binding AgregarNotaCommand}" />
        </StackPanel>

        <!-- DataTable -->
        <ui:DataGrid Grid.Row="1"
                     Name="NotasDataGrid"
                     Margin="50 0 50 0"
                     AutoGenerateColumns="False"
                     IsReadOnly="True"
                     ItemsSource="{Binding Notas}"
                     SelectedItem="{Binding Nota, Mode=TwoWay}">
            <ui:DataGrid.Columns>
                <DataGridTextColumn Header="Título" Binding="{Binding Titulo}" Width="*"/>
                <DataGridTextColumn Header="Contenido" Binding="{Binding Contenido}" Width="*"/>
                <DataGridTextColumn Header="Color" Binding="{Binding Color}" Width="*" />

                <DataGridTemplateColumn Header="Acciones" Width="Auto">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <ui:Button Command="{Binding DataContext.EliminarNotaCommand, RelativeSource={RelativeSource AncestorType={x:Type ui:DataGrid}}}" 
                                       CommandParameter="{Binding}"
                                       Appearance="Danger"
                                       Foreground="White"
                                       HorizontalAlignment="Center">
                                <ui:Button.Content>
                                    <TextBlock Text="&#xE74D;" 
                                               FontFamily="Segoe MDL2 Assets" 
                                               Foreground="White" 
                                               FontSize="16"
                                               VerticalAlignment="Center" 
                                               HorizontalAlignment="Center" />
                                </ui:Button.Content>
                            </ui:Button>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </ui:DataGrid.Columns>
        </ui:DataGrid>
    </Grid>
</Page>